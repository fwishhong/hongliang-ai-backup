# 2026-06-17

## 17:15 OpenClaw 更新 (yellow line - npm install -g)
- 旧版本: 2026.6.6 (8c802aa)
- 新版本: 2026.6.8 (844f405)
- 命令: `npm install -g openclaw@latest`
- 结果: changed 285 packages in 13s, exit 0
- Gateway 状态: 还在跑旧版本，需要重启才能生效
- Doctor warnings: brave 和 feishu 插件 metadata 冲突
- **待办**: 等老洪确认是否重启 gateway + 清理插件 metadata

## 23:07 AI记忆备份修复 (cron 触发)
- 第一次跑脚本失败：`/tmp/hongliang-ai-backup` 目录不存在（/tmp 在重启时会被清掉）
- 远程仓库 `https://github.com/fwishhong/hongliang-ai-backup.git` 状态：200 存在
- 用 `gh auth status` 确认登录账号是 `fwishhong`，keyring 有 token
- 修复：`git clone https://github.com/fwishhong/hongliang-ai-backup.git /tmp/hongliang-ai-backup`
- 重跑脚本：35 files changed, +3027 / -63，**已成功 push**（9fd097b → 2b84f3a）
- 距离上次备份（2026-04-07）已经 70+ 天，一次性补回了 2 个月的记忆
- **潜在风险**: /tmp 目录在 Mac 重启后会丢失，下次 cron 跑可能又失败
- **改进建议**: 后续考虑把 BACKUP_REPO 改到 `$HOME/.cache/` 或 workspace 内的 `.backup/` 目录，更稳定
